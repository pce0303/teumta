/**
 * 관광지 집중률 예측 데이터 API 원본 응답 타입.
 *
 * ⚠️ 아직 공식 스펙/실제 응답 샘플을 확인하지 않았다. 실제 필드를 추측하지 않는다.
 *
 * TODO: 공식 스펙 확인 후 실제 필드로 대체. (예상 후보)
 *   - 장소 식별자, 시간대별 예상 혼잡도 배열(시각 + 예측 등급/지수) 등
 */

/** 집중률 예측 조회 응답(원본 전체). */
export type PredictionResponse = Record<string, unknown>;
